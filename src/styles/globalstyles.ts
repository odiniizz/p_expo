import {StyleSheet} from 'react-native'

export const stylesg = StyleSheet.create({
    container: {
        flex: 1
    },

    texto: {
        color: '#000',
        textAlign: 'center',
        fontSize: 38,
        paddingTop: 70,
        fontFamily: 'Itim_400Regular',
        width: 400,
    },

    icone: {
        paddingTop: 200,
        alignItems: 'center'
    }
})